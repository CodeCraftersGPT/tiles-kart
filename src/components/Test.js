// sample useEffect with no dependencies

// Path: tiles-kart/src/components/Test.js

useEffect(() => {
  // Runs for the first time
  // Runs after every render when props or state changes
},[props,state])
